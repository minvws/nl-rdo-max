services:
  redis:
    image: redis:7.0.4
    restart: 'no'
    ports:
      - '16379:6379'
  redis-init:
    depends_on: [ 'redis' ]
    image: redis:7.0.4
    restart: 'no'
    command: 'redis-cli -h redis set max:primary_identity_provider tvs'
